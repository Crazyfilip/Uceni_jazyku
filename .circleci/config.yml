version: 2.1
executors:
  dotnet:
    docker:
      - image: mcr.microsoft.com/dotnet/core/sdk:3.0
    environment:
      DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
jobs:
  build:
    executor: dotnet
    steps:
      - checkout
      - run: dotnet restore
      - run: dotnet build